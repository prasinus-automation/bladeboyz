# Supabase (optional — game runs in guest mode without these).
# See docs/supabase-setup.md.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Game server port for `npm run dev:server` (vite proxies /ws to it).
PORT=3004
